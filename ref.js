function respondToPrompt(roomType, userReply) {
    if (roomType === 'monster') {
        if (userReply === "1") {
            // Fight the monster
        } else if (userReply === "2") {
            // Run from the monster
        }
    }
}